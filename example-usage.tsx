import { StyledButton } from "@/components/ui/styled-button"

// In your component:
;(<StyledButton>READ FEATURE</StyledButton>) <
  // With arrow:\
  StyledButton
withArrow={true}>READ
FEATURE
</StyledButton>
// Other variants:
;(<StyledButton variant="outline">OUTLINE BUTTON</StyledButton>) < StyledButton
variant = "ghost" > GHOST
BUTTON
</StyledButton>
